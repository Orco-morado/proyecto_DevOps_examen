apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: devopsVG
  labels:
    app: mysql
    tier: database
spec:
  type: ClusterIP
  selector:
    app: mysql
  ports:
    - name: mysql-port
      port: 3306
      targetPort: 3306
